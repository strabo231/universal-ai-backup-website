<!DOCTYPE html>
<html>

<head>
    <title>Universal AI Backup</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .hero {
            text-align: center;
            padding: 60px 20px;
            background: white;
            border-radius: 20px;
            margin-bottom: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .hero h1 {
            font-size: 3em;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 1.3em;
            color: #666;
            margin-bottom: 30px;
        }

        .download-btn {
            display: inline-block;
            padding: 15px 40px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-weight: bold;
            font-size: 1.1em;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
        }

        .features {
            background: white;
            padding: 40px;
            border-radius: 20px;
            margin-bottom: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .features h2 {
            text-align: center;
            font-size: 2em;
            margin-bottom: 30px;
            color: #764ba2;
        }

        .features ul {
            list-style: none;
            padding: 0;
        }

        .features li {
            padding: 15px 0;
            font-size: 1.1em;
            border-bottom: 1px solid #eee;
        }

        .features li:last-child {
            border-bottom: none;
        }

        .features li:before {
            content: "✓ ";
            color: #667eea;
            font-weight: bold;
            margin-right: 10px;
        }

        .pricing-section {
            background: white;
            padding: 40px;
            border-radius: 20px;
            margin-bottom: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .pricing-section h2 {
            text-align: center;
            font-size: 2em;
            margin-bottom: 40px;
            color: #764ba2;
        }

        .pricing {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-bottom: 20px;
        }

        .plan {
            border: 2px solid #e0e0e0;
            padding: 30px;
            border-radius: 15px;
            transition: all 0.3s ease;
            text-align: center;
            background: white;
        }

        .plan:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.2);
            border-color: #667eea;
        }

        .plan.featured {
            border-color: #667eea;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            transform: scale(1.05);
        }

        .plan.featured:hover {
            transform: translateY(-5px) scale(1.05);
        }

        .plan h3 {
            font-size: 1.5em;
            margin-bottom: 15px;
        }

        .price {
            font-size: 2em;
            font-weight: bold;
            margin: 20px 0;
            color: #667eea;
        }

        .plan.featured .price {
            color: white;
        }

        .plan ul {
            list-style: none;
            margin: 20px 0;
            padding: 0;
        }

        .plan li {
            padding: 10px 0;
            font-size: 1em;
        }

        .plan-btn {
            margin-top: 20px;
            padding: 12px 35px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            font-size: 1em;
            font-weight: bold;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .plan-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
        }

        .plan.featured .plan-btn {
            background: white;
            color: #667eea;
        }

        .plan.featured .plan-btn:hover {
            background: #f8f9fa;
        }

        .support {
            background: white;
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            margin-bottom: 20px;
        }

        .support h2 {
            font-size: 2em;
            margin-bottom: 20px;
            color: #764ba2;
        }

        .support p {
            font-size: 1.2em;
            color: #666;
        }

        .support a {
            color: #667eea;
            text-decoration: none;
            font-weight: bold;
        }

        footer {
            text-align: center;
            padding: 20px;
            color: white;
        }

        footer a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            opacity: 0.9;
            transition: opacity 0.3s ease;
        }

        footer a:hover {
            opacity: 1;
            text-decoration: underline;
        }

        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 1000;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: white;
            padding: 40px;
            border-radius: 20px;
            max-width: 450px;
            width: 90%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.3s ease;
        }

        @keyframes slideIn {
            from {
                transform: translate(-50%, -60%);
                opacity: 0;
            }
            to {
                transform: translate(-50%, -50%);
                opacity: 1;
            }
        }

        .modal-content h3 {
            font-size: 1.8em;
            margin-bottom: 10px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .modal-content p {
            color: #666;
            margin-bottom: 20px;
        }

        .modal-content input {
            width: 100%;
            padding: 15px;
            margin: 10px 0 20px 0;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 1em;
            transition: border-color 0.3s ease;
        }

        .modal-content input:focus {
            outline: none;
            border-color: #667eea;
        }

        .modal-buttons {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .modal-btn {
            flex: 1;
            padding: 12px;
            border: none;
            border-radius: 10px;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .modal-btn.cancel {
            background: #e0e0e0;
            color: #666;
        }

        .modal-btn.cancel:hover {
            background: #d0d0d0;
        }

        .modal-btn.submit {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .modal-btn.submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
        }

        .modal-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2em;
            }

            .pricing {
                grid-template-columns: 1fr;
            }

            .plan.featured {
                transform: scale(1);
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="hero">
            <h1>Universal AI Backup</h1>
            <p>Professional conversation backup for AI platforms</p>
            <a href="#" class="download-btn">Download Extension</a>
        </div>

        <div class="features">
            <h2>Features</h2>
            <ul>
                <li>Backup Claude, ChatGPT, Gemini conversations</li>
                <li>Multiple export formats (JSON, Markdown, HTML, ZIP)</li>
                <li>Auto-backup scheduling (Pro tier)</li>
                <li>Secure cloud storage integration</li>
                <li>One-click export with keyboard shortcuts</li>
                <li>Cross-platform conversation management</li>
            </ul>
        </div>

        <div class="pricing-section">
            <h2>Choose Your Plan</h2>
            <div class="pricing">
                <div class="plan" onclick="selectPlan('free')">
                    <h3>Free</h3>
                    <p class="price">$0<span style="font-size:0.5em">/month</span></p>
                    <ul>
                        <li>10 exports/day</li>
                        <li>Basic formats</li>
                        <li>Manual backup</li>
                    </ul>
                    <button class="plan-btn">Get Started</button>
                </div>

                <div class="plan featured" onclick="selectPlan('starter')">
                    <h3>Starter</h3>
                    <p class="price">$19<span style="font-size:0.5em">/month</span></p>
                    <ul>
                        <li>100 exports/month</li>
                        <li>3 platforms</li>
                        <li>All formats</li>
                    </ul>
                    <button class="plan-btn">Subscribe</button>
                </div>

                <div class="plan" onclick="selectPlan('pro')">
                    <h3>Pro</h3>
                    <p class="price">$39<span style="font-size:0.5em">/month</span></p>
                    <ul>
                        <li>Unlimited exports</li>
                        <li>All platforms</li>
                        <li>Auto-backup</li>
                    </ul>
                    <button class="plan-btn">Subscribe</button>
                </div>
            </div>
        </div>

        <div class="support">
            <h2>Support</h2>
            <p>Need help? Contact us at <a href="mailto:aibackuppro@gmail.com">aibackuppro@gmail.com</a></p>
        </div>
    </div>

    <footer>
        <a href="privacy.html">Privacy Policy</a> |
        <a href="terms.html">Terms of Service</a>
        <p style="margin-top: 10px; opacity: 0.8;">© 2024 Universal AI Backup. All rights reserved.</p>
    </footer>

    <!-- Email Modal -->
    <div id="emailModal" class="modal">
        <div class="modal-content">
            <h3>Subscribe to <span id="modalTier"></span></h3>
            <p>Enter your email to continue to checkout:</p>
            <input type="email" id="emailInput" placeholder="your@email.com" autocomplete="email">
            <div class="modal-buttons">
                <button class="modal-btn cancel" onclick="closeEmailModal()">Cancel</button>
                <button class="modal-btn submit" id="submitBtn" onclick="submitEmail()">Continue to Checkout</button>
            </div>
        </div>
    </div>

    <script>
        let selectedTier = '';

        function selectPlan(tier) {
            if (tier === 'free') {
                alert('Download the extension from Chrome Web Store to get started with the free tier!');
                // TODO: Update with actual Chrome Web Store link
                // window.location.href = 'https://chrome.google.com/webstore/detail/YOUR_EXTENSION_ID';
                return;
            }

            selectedTier = tier;
            const tierName = tier.charAt(0).toUpperCase() + tier.slice(1);
            document.getElementById('modalTier').textContent = tierName;
            document.getElementById('emailModal').style.display = 'block';
            
            setTimeout(() => {
                document.getElementById('emailInput').focus();
            }, 100);
        }

        function closeEmailModal() {
            document.getElementById('emailModal').style.display = 'none';
            document.getElementById('emailInput').value = '';
            selectedTier = '';
        }

        function isValidEmail(email) {
            const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            return re.test(email);
        }

        async function submitEmail() {
            const email = document.getElementById('emailInput').value.trim();
            const submitBtn = document.getElementById('submitBtn');

            if (!email) {
                alert('Please enter your email address.');
                return;
            }

            if (!isValidEmail(email)) {
                alert('Please enter a valid email address.');
                return;
            }

            // Disable button and show loading state
            submitBtn.disabled = true;
            submitBtn.textContent = 'Processing...';

            try {
                const response = await fetch('https://universal-ai-backup-server-production.up.railway.app/create-checkout', {
                    method: 'POST',
                    headers: { 
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ 
                        tier: selectedTier, 
                        email: email 
                    })
                });

                const data = await response.json();

                if (data.url) {
                    // Redirect to Stripe checkout
                    window.location.href = data.url;
                } else if (data.error) {
                    alert('Error: ' + data.error);
                    submitBtn.disabled = false;
                    submitBtn.textContent = 'Continue to Checkout';
                } else {
                    alert('Checkout setup in progress. Please try again in a moment.');
                    submitBtn.disabled = false;
                    submitBtn.textContent = 'Continue to Checkout';
                }
            } catch (error) {
                console.error('Checkout error:', error);
                alert('Unable to process payment at this time. Please contact support at aibackuppro@gmail.com');
                submitBtn.disabled = false;
                submitBtn.textContent = 'Continue to Checkout';
            }
        }

        // Allow Enter key to submit
        document.getElementById('emailInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                submitEmail();
            }
        });

        // Close modal when clicking outside
        document.getElementById('emailModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeEmailModal();
            }
        });
    </script>
</body>

</html>
